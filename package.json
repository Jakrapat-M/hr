{
  "name": "central-hrms",
  "version": "1.0.0",
  "private": true,
  "description": "Central Retail Next-Gen HRMS — Next.js frontend workspace",
  "workspaces": [
    "src/frontend"
  ],
  "scripts": {
    "audit:fields": "tsx scripts/audit-field-coverage.ts",
    "dev": "npm run dev --workspace src/frontend",
    "build": "npm run build --workspace src/frontend",
    "test": "npm run test --workspace src/frontend",
    "lint": "npm run lint --workspace src/frontend",
    "test:e2e": "npm run test:e2e --workspace src/frontend"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "concurrently": "^9.0.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "playwright": "^1.57.0",
    "prettier": "^3.4.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
